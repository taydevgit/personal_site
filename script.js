// Target the video element using its CSS class name
const spaceVideo = document.querySelector('.space-bg-video');

// Set the playback rate (0.5 makes it slower)
if (spaceVideo) {
    spaceVideo.playbackRate = 0.5;
}